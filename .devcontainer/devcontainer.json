{
    "name": "Bear AI developer in Go for local development",
    "build": {
        "dockerfile": "Dockerfile",
        "context": "..",
        "target": "dev",
        "options": [
            "--pull"
        ]
    },
    "customizations": {
        "vscode": {
            "settings": {
                "debug.allowBreakpointsEverywhere": true,
                "editor.codeActionsOnSave": {
                    "source.organizeImports": "always"
                }
            },
            "extensions": [
                "github.copilot-chat",
                "golang.go"
            ]
        }
    },
    "workspaceMount": "source=bear-go-workspace-volume,target=/workspace,type=volume",
    "workspaceFolder": "/workspace",
    "mounts": [
        "source=bear-go-persist-volume,target=/persist,type=volume"
    ],
    "remoteUser": "root",
    "containerEnv": {
        "TZ": "Asia/Seoul",
        "IS_SANDBOX": "1",
        "ENABLE_LSP_TOOL": "1",
        "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1",
        "CLAUDE_CODE_DISABLE_FEEDBACK_SURVEY": "1",
        "CLAUDE_CODE_ADDITIONAL_DIRECTORIES_CLAUDE_MD": "1"
    },
    "initializeCommand": "${containerWorkspaceFolder}/.devcontainer/scripts/initialize_script.sh \"${containerWorkspaceFolder}\"",
    "postCreateCommand": "${containerWorkspaceFolder}/.devcontainer/scripts/post_create_script.sh \"${containerWorkspaceFolder}\"",
    "postStartCommand": "${containerWorkspaceFolder}/.devcontainer/scripts/post_start_script.sh \"${containerWorkspaceFolder}\""
}